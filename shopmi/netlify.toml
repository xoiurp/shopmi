[build]
  base = "shopmi"
  publish = ".next" # Corrigido o caminho do diretório de publish
  command = "npm run build"
  # Omitir a verificação de segredos em diretórios de cache e build
  secrets_scan_omit_paths = [
    ".next/**/*",
    ".netlify/**/*"
  ]

[[plugins]]
package = "@netlify/plugin-nextjs"
